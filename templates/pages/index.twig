{% extends "_base/page" %}
{% block content %}
    <article eager-load-css="pages">
        <section>
            <h1>{{ entry.title }}</h1>
            {{ entry.body }}
        </section>
    </article>
{% endblock %}